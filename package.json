{
  "name": "kib-git-quickview",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "A Chrome extension that turns any GitHub username into a compact developer dashboard — repo count, latest repos, public activity, top languages — in one click, without leaving the current page.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --ext .ts",
    "test": "vitest run"
  },
  "devDependencies": {
    "eslint": "^10.8.1",
    "typescript": "~7.0.2",
    "vite": "^8.2.1"
  }
}